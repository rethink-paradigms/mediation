name: pi-agent-designer
model: deepseek/deepseek-v4-pro
thinking: high
max_concurrency: 1
task_timeout_minutes: 30

tools:
  builtin: [read, write, bash, ls, find, grep]
  custom: [web_search, web_fetch, navigate]

extensions:
  - context-memory
  - openworkflow-dispatch

prompt: prompt.md
