name: email
model: deepseek/deepseek-v4-flash
thinking: off
max_concurrency: 1

tools:
  builtin: [read, bash, write, edit, ls, find, grep]
  custom: [email_pull, email_query, email_read, email_thread, email_send, email_reply, email_flag]

extensions:
  - context-memory

prompt: prompt.md
